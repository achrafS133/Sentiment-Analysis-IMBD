from __future__ import annotations
import os

# Config file paths (relative to repository root)
CONFIG_FILE_PATH: str = os.path.join("config", "config.yaml")
PARAMS_FILE_PATH: str = os.path.join("params.yaml")
